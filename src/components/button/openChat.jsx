export default function OpenChat({ openToChat }) {
    return (
        <>
            <h1>Thanks for using our service</h1>
            <button onClick={openToChat} type="button">Open Chat</button>
        </>
    )
}